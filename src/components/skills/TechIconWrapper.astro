---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<span
  class="block w-10 h-10 opacity-50 transform transition-all duration-150 hover:scale-105 hover:opacity-100"
  title={title}
>
  <slot />
</span>
